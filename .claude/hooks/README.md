# Hooks

This starter package ships only with a safe Windows notification example in `.claude/settings.example.json`.

Suggested next hooks after the stack is chosen:
- protect sensitive files from Edit/Write
- auto-format edited files
- lightweight post-test summarization
- audit config changes
