// Design Preview Screen - Shows the new modern design direction
// Access via navigating to '/design-preview'

import 'package:flutter/material.dart';
import '../design_example.dart';

class DesignPreviewScreen extends StatelessWidget {
  const DesignPreviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const ModernDesignExample();
  }
}
